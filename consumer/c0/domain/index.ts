/**
 * Framework-free consumer domain modules belong here.
 *
 * Phase 0 deliberately leaves this barrel empty rather than moving or wrapping
 * legacy models before their behavior has characterization coverage.
 */
export {};
